$("#inspire-form-<%= @image.id%>").html("<%= escape_javascript(render partial: 'inspired', locals: {image: @image}) %>");
$('#inspired-count-<%= @image.id%>').html(
	"Inspired <%= pluralize(@image.inspired_count, 'person') %>");