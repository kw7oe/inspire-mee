<div class = "col-md-6 col-md-offset-3">
	<% unless current_page?(image) %>
		<%= link_to image_tag(image.link, class: 'img-responsive'), image %>
	<% else %>
		<%= image_tag(image.link, class: 'img-responsive') %>
	<% end %>
</div>